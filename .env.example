# --- Docker Hub Config (Optional) ---
DOCKER_HUB_USERNAME=your_dockerhub_username

# --- Database Common Settings ---
# MySQL Root Password (For all databases)
MYSQL_ROOT_PASSWORD=secret_root_password

# Application Database User (Used by microservices to connect)
DB_USERNAME=library_user
DB_PASSWORD=secure_library_password

# --- Redis ---
REDIS_HOST=redis

# --- Google OAuth2 Credentials ---
# Enter the values obtained from Google Cloud Console here (into your local .env file).
# NEVER PUSH THESE CREDENTIALS TO GITHUB!
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# --- Service URLs (Internal Docker Network Communication) ---
# Usually, there is no need to change these
SERVICES_BOOK_URL=http://book-service:8081/api/books
SERVICES_MEMBER_URL=http://member-service:8082/api/members